import { TeachingBubbleDefinition } from "@fabric-msft/fabric-web";

const TeachingBubble = () => /* html */ ``;

export default {
  definition: TeachingBubbleDefinition.define(customElements),
  component: TeachingBubble,
};
